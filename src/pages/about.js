import * as React from "react"
import Layout from "../components/layout"

export default function About() {
  return (
    <Layout pageTitle="About Me">
      <p>Just a person who can't resist the urge to build and break websites.</p>
    </Layout>
  )
}